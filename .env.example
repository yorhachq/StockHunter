# 应用展示名称，会出现在页面标题、浏览器标签和文档中。
NEXT_PUBLIC_APP_NAME=StockHunter

# PGlite 数据库存放目录。默认使用项目内相对路径，便于本地独立运行。
STOCKHUNTER_DB_DIR=./data/stockhunter-db

# 是否在数据库为空时自动写入演示数据。
# true: 首次启动自动生成一套示例交易记录
# false: 保持空库，由你自行录入真实数据
STOCKHUNTER_AUTO_SEED=false
